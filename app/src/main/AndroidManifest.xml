<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UnyieldingMight">
        <activity
            android:name=".Activities.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.UnyieldingMight">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.LoginActivity"></activity>
        <activity android:name=".Activities.RegisterActivity"></activity>
        <activity android:name=".Activities.VerifyActivity"></activity>
        <activity android:name=".Activities.MembershipActivity"></activity>
        <activity android:name=".Activities.ProfileActivity"></activity>
        <activity android:name=".Activities.LinkmembershipActivity"></activity>
        <activity android:name=".Activities.ActivitylevelActivity"></activity>
        <activity android:name=".Activities.StatsActivity"></activity>
        <activity android:name=".Activities.DoneActivity"></activity>
        <activity android:name=".Activities.HomeActivity"></activity>
        <activity android:name=".Activities.InfoActivity"></activity>
        <activity android:name=".Activities.EditClassActivity"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>